/**
 * Feed Filter Bar Component
 *
 * Allows filtering the feed by discipline, post type, and tags (RF-FD-02).
 *
 * TODO: Implement filter UI with discipline pills and type selector.
 * Assigned to: [TEAM MEMBER]
 */
export function FeedFilters() {
  return (
    <div className="flex flex-wrap gap-2 p-3 rounded-lg bg-muted/50">
      <p className="text-sm text-muted-foreground">
        TODO: FeedFilters component — RF-FD-02
      </p>
    </div>
  );
}

