/**
 * Spark Card Component — Feed Item
 *
 * Displays a Spark in the feed with inline resource previews (RF-SP-05).
 * Shows: author, content, resources, reactions, fork count, time.
 *
 * TODO: Implement full card with resource renderers.
 * Assigned to: [TEAM MEMBER]
 */
export function SparkCard() {
  return (
    <article className="rounded-xl border border-border bg-card p-4 space-y-3 hover:border-spark/50 transition-colors">
      <p className="text-sm text-muted-foreground">
        TODO: SparkCard component — RF-SP-05
      </p>
    </article>
  );
}
